global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/alert_rules.yml

scrape_configs:
  - job_name: "nginx"
    static_configs:
      - targets: ["host.docker.internal:9113"]
        labels:
          service: "blog"
          env: "homelab"
